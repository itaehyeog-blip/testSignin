# Rules

- Do not create or manage MCP server configurations inside the workspace (`.agents/mcp_config.json`). The user will manage MCP server configurations directly via the global configuration file.
